# Smithery configuration file
# https://smithery.ai/docs/deployments

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: 'node',
      args: ['dist/index.js']
    })
